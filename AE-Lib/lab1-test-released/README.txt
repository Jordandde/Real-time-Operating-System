This is ECE350 S22 P1 Private Test Suites.
The test_spec.md contains the test specification
The TS-Lib/G99 contains the Test suites' .lib files.
To re-run a test, replace your own .lib files with the test suite's .lib files.

Your lab grade is at row 4, column 3. 
The row 5 is the raw points you got for running the code either on simulator or the hardware without applying simulator only penalty. 

